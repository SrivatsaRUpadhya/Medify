div(class="container max-w-sm rounded overflow-hidden shadow-lg my-2")
	img(class="w-full" src=`${medicine.image}`)
	div(class="px-6 py-4")
		div(class="font-bold text-xl mb-2")= medicine.name
		p(class=" text-gray-700 text-base")= "Medicine for: " + medicine.for
		p(class="text-gray-700 text-base")=  "Schedule " + medicine.description
		p(class="text-gray-700 text-base")=  "Dosage" + medicine.description
		p(class="text-gray-700 text-base")=  "Description" + medicine.description

